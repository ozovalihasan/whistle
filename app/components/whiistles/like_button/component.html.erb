<div class="flex items-center">
    
    <div class="<%= whiistle_like_button_target(@whiistle) %>">
        <% if @current_user_presenter.liked? @whiistle %>
            <%= render Whiistles::DestroyLikeButton::Component.new(like: @current_user_presenter.like_of(@whiistle)) %>
        <% else %>
            <%= render Whiistles::CreateLikeButton::Component.new(whiistle: @whiistle) %>
        <% end %>
    </div>

    
</div>
