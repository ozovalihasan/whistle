<div class="pl-2 pt-1">
  <%= render Whiistles::WhiistleComponent.new(whiistle: @flood, current_user_presenter: @current_user_presenter) %>
</div>

<% if @flood.flood.present? %>
  <%= render Whiistles::AllChildrenFloodsComponent.new(flood: @flood.flood, current_user_presenter: @current_user_presenter) %>
<% end %>