<%= button_to whiistle_likes_path(@whiistle), id: dom_id(@whiistle, :like), class: "p-2" do %>
    <i class="bi bi-heart text-first ">
      <span><%= @whiistle.likes.size %></span> 
    </i>
  <% end %>