<% if @relation %>
    <%= render component("users/delete_following", relation: @relation) %>
<% elsif @cur_user.id != @user.id %>
    <%= render component("users/add_following", user_id: @user.id) %>   
<% end %>