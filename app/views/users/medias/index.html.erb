<% content_for :tabs do %>
  <%= render "users/medias_frame", user: @user, paginated_whiistles: @paginated_whiistles %>
<% end %>

<%= render partial: "users/tabs", locals: { selected_tab: :medias, user: @user, relation: @relation } %>