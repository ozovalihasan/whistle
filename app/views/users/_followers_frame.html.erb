<turbo-frame id=<%= dom_id(@user, :followers) %> >
    <% if @followeds.present? %>
        <%= render 'users/list_users', users: @followeds  %>
    <% else %>
        <%= @user.fullname %> doesn't have any follower.
    <% end %>
</turbo-frame>