<% if @rewhiistle.saved_changes? %>
  <%= turbo_stream.update_all classes_including whiistle_rewhiistle_button_target(@rewhiistle.whiistle) do %>
    <%= render Whiistles::DestroyRewhiistleButtonComponent.new(rewhiistle: @rewhiistle) %>
  <% end %>

  <%= turbo_stream.update_all classes_including whiistle_rewhiistle_or_quote_button_target(@rewhiistle.whiistle) do %>
    <%= render Whiistles::DestroyRewhiistleOrQuoteButtonComponent.new(whiistle: @rewhiistle.whiistle) %>
  <% end %>
<% end %>

<%= render Streams::UpdateFlashesComponent.new(notice: notice, alert: alert) %>