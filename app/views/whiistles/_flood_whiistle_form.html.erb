<%= form.input :body, as: :text, label: "Flood", wrapper: :whiistle_wrapper %>

<%= form.input :quoted_whiistle_url, as: :string, placeholder: "Copy paste the link of a whiistle to quote" %>

<div class="w-full flex">
  <%= render "whiistles/form_pictures_button", form: form %>
</div>