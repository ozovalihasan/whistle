<% if whiistle.respond_to?(:label) && whiistle.label == "shared_whiistle" %>
  <div class="my-1">
    <i class="bi bi-repeat">
      <%= (whiistle.respond_to?(:parent_user) && whiistle.parent_user) || @user.fullname %> Rewhiistled    
    </i>
  </div>    
<% end %>